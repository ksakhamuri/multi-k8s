sudo: required
services:
  - docker

before_install:
  - docker build -t ksakhamuri/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run ksakhamuri/react-test npm test -- --coverage

after_success:
  - docker build -t ksakhamuri/multi-client ./client
  - docker build -t ksakhamuri/multi-nginx ./nginx
  - docker build -t ksakhamuri/multi-server ./server
  - docker build -t ksakhamuri/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push ksakhamuri/multi-client
  - docker push ksakhamuri/multi-nginx
  - docker push ksakhamuri/multi-server
  - docker push ksakhamuri/multi-worker